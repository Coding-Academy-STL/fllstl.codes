// types
import type { NextPage } from 'next'

// components
import Navbar from '../components/navbar';
import Layout from '../components/layout';

const Home : NextPage = () => {
    return (
        <Layout active='home'><></></Layout>
    );
}

export default Home;